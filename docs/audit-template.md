# {{项目名}} 逐行代码审计

> 使用方式：复制本模板为 `docs/audits/<org-repo>.md`（如 `docs/audits/vxcontrol-pentagi.md`），逐节填写。

## 0. 元信息

| 项 | 值 |
|---|---|
| 仓库地址 | https://github.com/{{org}}/{{repo}} |
| 本地路径 | `repos/{{category}}/{{repo}}/` |
| 审计基线 commit | `{{git rev-parse HEAD}}`（审计基于的代码版本） |
| 语言 / 规模 | 如：Python，约 N 个文件 / M 行 |
| Landscape 定位 | 类型：渗透 Agent / Stars：xx / 一句话简介 |
| 关联论文 | 如有（arXiv 编号 + 链接） |
| 审计日期 / 人 | |

## 1. 项目解决什么问题

- 目标场景（Web 渗透 / AD / CTF / 漏洞挖掘 / LLM 红队…）
- 输入输出（用户给什么 → 系统产出什么：报告 / shell / PoC / 漏洞清单）
- 与同类项目的差异化定位

## 2. 架构总览

- 顶层模块图（用文字/mermaid 描述模块与数据流）
- 单 Agent 还是多 Agent？编排方式（ReAct / Plan-Execute / 状态机 / 黑板…）
- LLM 调用层（支持哪些模型、如何抽象）
- 执行环境（本地 shell / Docker / 远程工具 / 浏览器 / MCP）

## 3. 目录结构逐层解读

```
repo/
├── path/        # 作用一句话
└── ...
```

## 4. 核心模块逐行精读（审计主体）

> 每个子节：贴关键代码路径 `file:line`，解释每段逻辑，记录值得注意的实现细节。

### 4.1 入口与初始化
### 4.2 Agent 编排 / 任务规划 / 状态管理
### 4.3 工具层（shell 执行、nmap/sqlmap 等工具封装、MCP、浏览器）
### 4.4 Prompt 设计（系统提示词、few-shot、上下文裁剪策略）
### 4.5 记忆与上下文管理（历史压缩、RAG、笔记文件）
### 4.6 结果验证与去误报机制
### 4.7 报告生成 / 人工交互（HITL）
### 4.8 安全与隔离（沙箱、授权范围检查、密钥管理）

## 5. 值得借鉴的设计与技巧

- 逐条列出可复用的工程手法（带代码位置）

## 6. 局限与改进点

- 架构短板、已知 issue、可改进方向

## 7. 与其他已审计项目的对比

| 维度 | 本项目 | 对比项目 |
|---|---|---|
| 编排方式 | | |
| 工具执行 | | |
| 验证机制 | | |
| 上下文管理 | | |

## 8. 文件级审计进度

| 路径 | 状态 | 备注 |
|---|---|---|
| `src/xxx.py` | ✅ 已读 | |
| ... | ⬜ | |

## 9. 结论（一句话：这个项目的核心实现思路是……）
